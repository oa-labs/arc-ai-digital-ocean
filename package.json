{
  "name": "ichat-ocean",
  "version": "1.0.0",
  "description": "AI-powered internal chat agent for workplace safety",
  "type": "module",
  "scripts": {
    "test:deepeval": "evalite tests/deepeval.eval.js",
    "build:shared": "cd lib && npm run build",
    "dev:shared": "cd lib && npm run dev",
    "clean:shared": "cd lib && npm run clean",
    "build:all": "npm run build:shared",
    "install:shared": "cd lib && npm install"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "autoevals": "^0.0.104",
    "dotenv": "^17.2.3",
    "evalite": "^0.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
