{
  "name": "arc-ai",
  "version": "1.0.58",
  "description": "AI-powered internal chat agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @arc-ai/shared dev",
    "clean": "pnpm -r clean",
    "test:deepeval": "vitest run tests/deepeval.test.js",
    "docker:clean": "docker image prune -a --force --filter \"until=120h\"",
    "build:containers": "pnpm --filter @arc-ai/web-ui run build:container && pnpm --filter @arc-ai/server run build:container && pnpm --filter @arc-ai/slack-bot run build:container",
    "push:containers": "pnpm --filter @arc-ai/web-ui run push:container && pnpm --filter @arc-ai/server run push:container && pnpm --filter @arc-ai/slack-bot run push:container"
  },
  "dependencies": {
    "@slack/bolt": "^4.5.0"
  },
  "devDependencies": {
    "autoevals": "^0.0.104",
    "dotenv": "^17.2.3",
    "vitest": "^2.0.0",
    "vitest-evals": "^0.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
