{
  "name": "ichat-ocean",
  "version": "1.0.0",
  "description": "AI-powered internal chat agent for workplace safety",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @ichat-ocean/shared dev",
    "clean": "pnpm -r clean",
    "test:deepeval": "evalite tests/deepeval.eval.js"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "autoevals": "^0.0.104",
    "dotenv": "^17.2.3",
    "evalite": "^0.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
