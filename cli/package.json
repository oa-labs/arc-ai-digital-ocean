{
  "name": "@arc-ai/cli",
  "version": "1.0.0",
  "description": "Command line tool for interacting with the ArcAI Ocean agent service",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "arcai-cli": "./bin/arcai-cli.js"
  },
  "files": [
    "dist/",
    "bin/",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && node ./scripts/create-bin.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist bin",
    "type-check": "tsc --noEmit",
    "start": "node ./dist/index.js",
    "prepublishOnly": "npm run build",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@arc-ai/shared": "workspace:*",
    "dotenv": "^17.2.3",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
